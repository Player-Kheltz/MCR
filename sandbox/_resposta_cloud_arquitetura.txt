MODEL MESH ARCHITECTURE:

[Cliente] -> CDN (Cloudflare) -> Global Load Balancer (AWS Route53 / GCP Cloud DNS)
                                              |
                                    Model Router Service
                                    (classifica complexidade)
                                    |
            +-----------------------+-----------------------+
            |                       |                       |
      [7B Pool]              [13B Pool]             [70B Pool]
    (America Sul)          (America Norte)          (Europa)
    70% req, $0.001/req    20% req, $0.005/req     10% req, $0.05/req
    Modelo: qwen2.5-coder   Modelo: llama3.1:8b     Modelo: deepseek-r1
            |                       |                       |
            +-----------------------+-----------------------+
                                    |
                          Redis Cluster Global
                        (consistencia eventual)
                      Cache TTL: 1h (simples), 24h (medio)
                      Cache hit rate: 40%
                                    |
                              Data Lake (S3/BigQuery)

TOLERANCIA A FALHAS: Blue-Green deploy, canary 10-50-100%. Rollback automatico se latencia >200ms. Se um DC cai, GLB redireciona para o mais proximo. Modelos 7B: N+2, 13B: N+1, 70B: N+1.

METRICAS: p50/p95/p99 latencia, cache hit rate, custo/req, erros por DC, precisao do Model Router.
