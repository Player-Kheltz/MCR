<UserControl x:Class="MCR.Grimorio.Modules.Database.DatabaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             Background="{StaticResource BgDarkestBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="12,8,12,4">
            <TextBlock Text="Banco de Dados" FontSize="20"
                       FontWeight="SemiBold"
                       Foreground="{StaticResource FgBrightBrush}" />
            <TextBlock Text="Tabelas, jogadores e dados do servidor"
                       FontSize="12"
                       Foreground="{StaticResource FgSecondaryBrush}"
                       Margin="0,2,0,0" />
        </StackPanel>

        <!-- Content -->
        <Grid Grid.Row="1" Margin="12,0,12,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="240" MinWidth="160" />
                <ColumnDefinition Width="6" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Left: Table List -->
            <Border Grid.Column="0"
                    Background="{StaticResource BgMediumBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1" CornerRadius="4">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="{StaticResource BgLightBrush}"
                            Padding="10,6" CornerRadius="4,4,0,0">
                        <TextBlock Text="Tabelas" FontSize="12"
                                   Foreground="{StaticResource FgSecondaryBrush}" />
                    </Border>

                    <TextBox Grid.Row="1" x:Name="TableSearchBox"
                             Margin="6,4,6,4" Padding="6,4"
                             FontSize="12"
                             TextChanged="TableSearchBox_TextChanged" />

                    <ListBox Grid.Row="2" x:Name="TableList"
                             Background="Transparent"
                             BorderThickness="0"
                             SelectionChanged="TableList_SelectionChanged"
                             FontSize="13" Margin="4,0,4,4"
                             ScrollViewer.HorizontalScrollBarVisibility="Auto"
                             ScrollViewer.VerticalScrollBarVisibility="Auto" />
                </Grid>
            </Border>

            <GridSplitter Grid.Column="1" Width="6" HorizontalAlignment="Stretch"
                          Background="{StaticResource BgDarkestBrush}"
                          BorderBrush="{StaticResource BorderBrush}" BorderThickness="0" />

            <!-- Right: Table Data -->
            <Border Grid.Column="2"
                    Background="{StaticResource BgMediumBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1" CornerRadius="4">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <!-- Table header -->
                    <Border Grid.Row="0" Background="{StaticResource BgLightBrush}"
                            Padding="10,6" CornerRadius="4,4,0,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBlock x:Name="TableTitle" Text="Selecione uma tabela"
                                       FontSize="13"
                                       Foreground="{StaticResource FgSecondaryBrush}"
                                       VerticalAlignment="Center" />

                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <TextBox x:Name="SearchBox" Width="160"
                                         FontSize="12" Margin="0,0,6,0"
                                         Padding="6,3" />
                                <Button x:Name="SearchButton" Content="Buscar"
                                        FontSize="11" Padding="8,2"
                                        Click="SearchButton_Click" />
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Data grid -->
                    <Border Grid.Row="1" Margin="0">
                        <ScrollViewer HorizontalScrollBarVisibility="Auto"
                                      VerticalScrollBarVisibility="Auto">
                            <DataGrid x:Name="DataGrid"
                                      Background="Transparent"
                                      AutoGenerateColumns="True"
                                      IsReadOnly="True"
                                      RowHeight="26"
                                      FontSize="12"
                                      BorderThickness="0"
                                      Margin="4" />
                        </ScrollViewer>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
