<UserControl x:Class="MCR.Grimorio.Modules.Scripts.ScriptsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BgDarkestBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Scripts" FontSize="24" FontWeight="SemiBold" Foreground="{StaticResource FgBrightBrush}" />
            <TextBlock x:Name="ScriptCountText" FontSize="13" Foreground="{StaticResource FgSecondaryBrush}" Margin="0,4,0,0" />
        </StackPanel>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Background="{StaticResource BgMediumBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4" Margin="0,0,8,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Background="{StaticResource BgLightBrush}" Padding="10,8" CornerRadius="4,4,0,0">
                        <TextBlock Text="Diretórios" FontSize="12" Foreground="{StaticResource FgSecondaryBrush}" />
                    </Border>
                    <TextBox Grid.Row="1" x:Name="ScriptSearchBox" Margin="8,6,8,4" Padding="6,4" FontSize="12" TextChanged="ScriptSearchBox_TextChanged" />
                    <TreeView Grid.Row="2" x:Name="ScriptTree" Background="Transparent" BorderThickness="0" SelectedItemChanged="ScriptTree_SelectedItemChanged" FontSize="12" />
                </Grid>
            </Border>
            <Border Grid.Column="1" Background="{StaticResource BgMediumBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Background="{StaticResource BgLightBrush}" Padding="10,8" CornerRadius="4,4,0,0">
                        <TextBlock x:Name="ScriptTitle" Text="Selecione um script" FontSize="13" Foreground="{StaticResource FgSecondaryBrush}" />
                    </Border>
                    <TextBox Grid.Row="1" x:Name="ScriptContent" IsReadOnly="True" FontFamily="Consolas" FontSize="12" Background="{StaticResource BgDarkBrush}" Foreground="{StaticResource FgPrimaryBrush}" BorderThickness="0" VerticalScrollBarVisibility="Auto" TextWrapping="NoWrap" />
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
