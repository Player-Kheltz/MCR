<UserControl x:Class="MCR.Grimorio.Modules.Deploy.DeployView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BgDarkestBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="{StaticResource BgMediumBrush}" BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1" CornerRadius="6" Padding="14,10" Margin="0,0,0,10">
            <StackPanel>
                <TextBlock Text="🚀 Deploy" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource FgBrightBrush}" />
                <TextBlock Text="Git + Build + Deploy em 1 clique" FontSize="11" Foreground="{StaticResource FgSecondaryBrush}" />
            </StackPanel>
        </Border>
        <Border Grid.Row="1" Background="{StaticResource BgMediumBrush}" BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1" CornerRadius="6" Padding="14" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Width="140" Margin="0,0,8,0">
                    <TextBlock Text="Mensagem do Commit" FontSize="11" Foreground="{StaticResource FgMutedBrush}" />
                    <TextBox x:Name="CommitMsgBox" Text="auto-update" Margin="0,2,0,0" FontSize="12" />
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="8,0">
                    <Button x:Name="GitStatusButton" Content="📋 Status" Height="30" Width="80" Margin="0,0,4,0" Click="GitStatusButton_Click" />
                    <Button x:Name="GitCommitButton" Content="💾 Commit" Height="30" Width="80" Margin="4,0" Click="GitCommitButton_Click" />
                    <Button x:Name="BuildButton" Content="🔨 Compilar" Style="{StaticResource PrimaryButton}" Height="30" Width="90" Margin="4,0" Click="BuildButton_Click" />
                    <Button x:Name="DeployButton" Content="🚀 Deploy" Style="{StaticResource DangerButton}" Height="30" Width="90" Margin="4,0,0,0" Click="DeployButton_Click" />
                </StackPanel>
                <StackPanel Grid.Column="2" Margin="8,0,0,0" Width="100">
                    <TextBlock Text="Status" FontSize="11" Foreground="{StaticResource FgMutedBrush}" />
                    <TextBlock x:Name="DeployStatusText" Text="Pronto" FontSize="12" Foreground="{StaticResource AccentGreenBrush}" />
                </StackPanel>
            </Grid>
        </Border>
        <TextBox Grid.Row="2" x:Name="DeployOutput" IsReadOnly="True" FontFamily="Consolas" FontSize="11"
                 Background="{StaticResource BgDarkBrush}" Foreground="{StaticResource FgPrimaryBrush}"
                 BorderThickness="0" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" />
    </Grid>
</UserControl>
