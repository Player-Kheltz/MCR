<UserControl x:Class="MCR.Grimorio.Modules.MultiPiso.MultiPisoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BgDarkestBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="{StaticResource BgMediumBrush}" BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1" CornerRadius="6" Padding="14,10" Margin="0,0,0,10">
            <StackPanel>
                <TextBlock Text="🏔️ Multi-Piso" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource FgBrightBrush}" />
                <TextBlock x:Name="DetectionText" FontSize="11" Foreground="{StaticResource AccentGreenBrush}" />
            </StackPanel>
        </Border>
        <Border Grid.Row="1" Background="{StaticResource BgMediumBrush}" BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1" CornerRadius="6" Padding="14" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <StackPanel Width="160" Margin="0,0,12,0">
                    <TextBlock Text="Limite Perseguidores" FontSize="11" Foreground="{StaticResource FgMutedBrush}" />
                    <TextBox x:Name="PursuitBox" Text="15" Margin="0,2,0,0" />
                </StackPanel>
                <StackPanel Width="160" Margin="0,0,12,0">
                    <TextBlock Text="Congestionamento" FontSize="11" Foreground="{StaticResource FgMutedBrush}" />
                    <TextBox x:Name="CongestBox" Text="5" Margin="0,2,0,0" />
                </StackPanel>
                <StackPanel Width="160" Margin="0,0,12,0">
                    <TextBlock Text="Jitter (ms)" FontSize="11" Foreground="{StaticResource FgMutedBrush}" />
                    <TextBox x:Name="JitterBox" Text="100-500" Margin="0,2,0,0" />
                </StackPanel>
                <Button x:Name="ScanButton" Content="🔍 Escanear" Height="30" VerticalAlignment="Bottom" Click="ScanButton_Click" />
            </StackPanel>
        </Border>
        <TextBox Grid.Row="2" x:Name="OutputBox" IsReadOnly="True" FontFamily="Consolas" FontSize="11"
                 Background="{StaticResource BgDarkBrush}" Foreground="{StaticResource FgPrimaryBrush}" BorderThickness="0" VerticalScrollBarVisibility="Auto" />
    </Grid>
</UserControl>
