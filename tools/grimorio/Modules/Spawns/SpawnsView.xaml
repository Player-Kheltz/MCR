<UserControl x:Class="MCR.Grimorio.Modules.Spawns.SpawnsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BgDarkestBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Spawns" FontSize="24" FontWeight="SemiBold" Foreground="{StaticResource FgBrightBrush}" />
            <TextBlock Text="Visualizador de spawns (OTBM e XML)" FontSize="13" Foreground="{StaticResource FgSecondaryBrush}" Margin="0,4,0,0" />
        </StackPanel>
        <Border Grid.Row="1" Background="{StaticResource BgMediumBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4" Padding="16">
            <StackPanel>
                <TextBlock Text="Para visualizar spawns, o Grimório precisa ler o arquivo OTBM do mapa." FontSize="13" />
                <TextBlock Text="Funcionalidade em desenvolvimento — usará parser OTBM portado do RME." FontSize="12" Foreground="{StaticResource FgMutedBrush}" Margin="0,4,0,16" />
                <TextBlock Text="Arquivos encontrados:" FontSize="12" Foreground="{StaticResource FgSecondaryBrush}" />

                <ListView x:Name="OtbmFilesList" Background="Transparent" BorderThickness="0" Height="200" Margin="0,8,0,0">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" FontSize="13" />
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
