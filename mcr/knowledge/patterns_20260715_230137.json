{
  "metadata": {
    "timestamp": "2026-07-15 23:01:37",
    "total_padroes": 1,
    "tipos": {
      "generic": 1,
      "total": 1
    }
  },
  "padroes": [
    {
      "arquivo": "<execucao:gerar>",
      "linguagem": "lua",
      "tipo": "generic",
      "api_calls": [
        "spec.get",
        "world_state: dict) -> dict",
        "world_state: dict) -> dict:\n    \"\"\"Cria uma entidade a partir de sua especificacao.\n    \n    Decisao de metodo:\n        NPC Tier 1: golden template (role generico, sem quest)\n        NPC Tier 2: codificar via pipeline (role especifico)\n        NPC Tier 3: codificar + expandir_npc (com quest)\n        Monster: golden template ou codificar\n        Quest: expandir_npc no giver\n    \n    Args:\n        spec: dict com type, name, role, etc. (vindo de idea_to_entity_spec)\n        world_state: estado acumulado (para registrar)\n    \n    Returns:\n        dict com sucesso, entidade, arquivo, tier, erros\n    \"\"\"\n    tipo = spec.get",
        "world_state: dict) -> dict:\n    \"\"\"Cria uma entidade a partir de sua especificacao.\n    \n    Decisao de metodo:\n        NPC Tier 1: golden template (role generico, sem quest)\n        NPC Tier 2: codificar via pipeline (role especifico)\n        NPC Tier 3: codificar + expandir_npc (com quest)\n        Monster: golden template ou codificar\n        Quest: expandir_npc no giver\n    \n    Args:\n        spec: dict com type, name, role, etc. (vindo de idea_to_entity_spec)\n        world_state: estado acumulado (para registrar)\n    \n    Returns:\n        dict com sucesso, entidade, arquivo, tier, erros\n    \"\"\"\n    tipo = spec.get('type', '')\n    nome = spec.get",
        "world_state: dict) -> dict:\n    \"\"\"Cria uma entidade a partir de sua especificacao.\n    \n    Decisao de metodo:\n        NPC Tier 1: golden template (role generico, sem quest)\n        NPC Tier 2: codificar via pipeline (role especifico)\n        NPC Tier 3: codificar + expandir_npc (com quest)\n        Monster: golden template ou codificar\n        Quest: expandir_npc no giver\n    \n    Args:\n        spec: dict com type, name, role, etc. (vindo de idea_to_entity_spec)\n        world_state: estado acumulado (para registrar)\n    \n    Returns:\n        dict com sucesso, entidade, arquivo, tier, erros\n    \"\"\"\n    tipo = spec.get('type', '')\n    nome = spec.get('name', '') or spec.get",
        "world_state: dict) -> dict:\n    \"\"\"Cria uma entidade a partir de sua especificacao.\n    \n    Decisao de metodo:\n        NPC Tier 1: golden template (role generico, sem quest)\n        NPC Tier 2: codificar via pipeline (role especifico)\n        NPC Tier 3: codificar + expandir_npc (com quest)\n        Monster: golden template ou codificar\n        Quest: expandir_npc no giver\n    \n    Args:\n        spec: dict com type, name, role, etc. (vindo de idea_to_entity_spec)\n        world_state: estado acumulado (para registrar)\n    \n    Returns:\n        dict com sucesso, entidade, arquivo, tier, erros\n    \"\"\"\n    tipo = spec.get('type', '')\n    nome = spec.get('name', '') or spec.get('title', '')\n    role = spec.get",
        "world_state: dict) -> dict:\n    \"\"\"Cria uma entidade a partir de sua especificacao.\n    \n    Decisao de metodo:\n        NPC Tier 1: golden template (role generico, sem quest)\n        NPC Tier 2: codificar via pipeline (role especifico)\n        NPC Tier 3: codificar + expandir_npc (com quest)\n        Monster: golden template ou codificar\n        Quest: expandir_npc no giver\n    \n    Args:\n        spec: dict com type, name, role, etc. (vindo de idea_to_entity_spec)\n        world_state: estado acumulado (para registrar)\n    \n    Returns:\n        dict com sucesso, entidade, arquivo, tier, erros\n    \"\"\"\n    tipo = spec.get('type', '')\n    nome = spec.get('name', '') or spec.get('title', '')\n    role = spec.get('role', '')\n\n    if not nome:\n        return",
        "world_state: dict) -> dict:\n    \"\"\"Cria uma entidade a partir de sua especificacao.\n    \n    Decisao de metodo:\n        NPC Tier 1: golden template (role generico, sem quest)\n        NPC Tier 2: codificar via pipeline (role especifico)\n        NPC Tier 3: codificar + expandir_npc (com quest)\n        Monster: golden template ou codificar\n        Quest: expandir_npc no giver\n    \n    Args:\n        spec: dict com type, name, role, etc. (vindo de idea_to_entity_spec)\n        world_state: estado acumulado (para registrar)\n    \n    Returns:\n        dict com sucesso, entidade, arquivo, tier, erros\n    \"\"\"\n    tipo = spec.get('type', '')\n    nome = spec.get('name', '') or spec.get('title', '')\n    role = spec.get('role', '')\n\n    if not nome:\n        return {'sucesso': False",
        "world_state: dict) -> dict:\n    \"\"\"Cria uma entidade a partir de sua especificacao.\n    \n    Decisao de metodo:\n        NPC Tier 1: golden template (role generico, sem quest)\n        NPC Tier 2: codificar via pipeline (role especifico)\n        NPC Tier 3: codificar + expandir_npc (com quest)\n        Monster: golden template ou codificar\n        Quest: expandir_npc no giver\n    \n    Args:\n        spec: dict com type, name, role, etc. (vindo de idea_to_entity_spec)\n        world_state: estado acumulado (para registrar)\n    \n    Returns:\n        dict com sucesso, entidade, arquivo, tier, erros\n    \"\"\"\n    tipo = spec.get('type', '')\n    nome = spec.get('name', '') or spec.get('title', '')\n    role = spec.get('role', '')\n\n    if not nome:\n        return {'sucesso': False, 'erro': 'Entidade sem nome', 'spec': spec}\n\n    # âââ NPC ââââââââââââââââââââââââââââââââââââââââââââââ\n    if tipo",
        "world_state: dict) -> dict:\n    \"\"\"Cria uma entidade a partir de sua especificacao.\n    \n    Decisao de metodo:\n        NPC Tier 1: golden template (role generico, sem quest)\n        NPC Tier 2: codificar via pipeline (role especifico)\n        NPC Tier 3: codificar + expandir_npc (com quest)\n        Monster: golden template ou codificar\n        Quest: expandir_npc no giver\n    \n    Args:\n        spec: dict com type, name, role, etc. (vindo de idea_to_entity_spec)\n        world_state: estado acumulado (para registrar)\n    \n    Returns:\n        dict com sucesso, entidade, arquivo, tier, erros\n    \"\"\"\n    tipo = spec.get('type', '')\n    nome = spec.get('name', '') or spec.get('title', '')\n    role = spec.get('role', '')\n\n    if not nome:\n        return {'sucesso': False, 'erro': 'Entidade sem nome', 'spec': spec}\n\n    # âââ NPC ââââââââââââââââââââââââââââââââââââââââââââââ\n    if tipo == 'npc':\n        tem_quest_associada",
        "world_state: dict) -> dict:\n    \"\"\"Cria uma entidade a partir de sua especificacao.\n    \n    Decisao de metodo:\n        NPC Tier 1: golden template (role generico, sem quest)\n        NPC Tier 2: codificar via pipeline (role especifico)\n        NPC Tier 3: codificar + expandir_npc (com quest)\n        Monster: golden template ou codificar\n        Quest: expandir_npc no giver\n    \n    Args:\n        spec: dict com type, name, role, etc. (vindo de idea_to_entity_spec)\n        world_state: estado acumulado (para registrar)\n    \n    Returns:\n        dict com sucesso, entidade, arquivo, tier, erros\n    \"\"\"\n    tipo = spec.get('type', '')\n    nome = spec.get('name', '') or spec.get('title', '')\n    role = spec.get('role', '')\n\n    if not nome:\n        return {'sucesso': False, 'erro': 'Entidade sem nome', 'spec': spec}\n\n    # âââ NPC ââââââââââââââââââââââââââââââââââââââââââââââ\n    if tipo == 'npc':\n        tem_quest_associada = bool(spec.get('quest'))\n        \n        if is_template_role(role) and not tem_quest_associada:\n            return _criar_npc_template",
        "world_state: dict) -> dict:\n    \"\"\"Cria uma entidade a partir de sua especificacao.\n    \n    Decisao de metodo:\n        NPC Tier 1: golden template (role generico, sem quest)\n        NPC Tier 2: codificar via pipeline (role especifico)\n        NPC Tier 3: codificar + expandir_npc (com quest)\n        Monster: golden template ou codificar\n        Quest: expandir_npc no giver\n    \n    Args:\n        spec: dict com type, name, role, etc. (vindo de idea_to_entity_spec)\n        world_state: estado acumulado (para registrar)\n    \n    Returns:\n        dict com sucesso, entidade, arquivo, tier, erros\n    \"\"\"\n    tipo = spec.get('type', '')\n    nome = spec.get('name', '') or spec.get('title', '')\n    role = spec.get('role', '')\n\n    if not nome:\n        return {'sucesso': False, 'erro': 'Entidade sem nome', 'spec': spec}\n\n    # âââ NPC ââââââââââââââââââââââââââââââââââââââââââââââ\n    if tipo == 'npc':\n        tem_quest_associada = bool(spec.get('quest'))\n        \n        if is_template_role(role) and not tem_quest_associada:\n            return _criar_npc_template(spec)\n        else:\n            return _criar_npc_codificado",
        "world_state: dict) -> dict:\n    \"\"\"Cria uma entidade a partir de sua especificacao.\n    \n    Decisao de metodo:\n        NPC Tier 1: golden template (role generico, sem quest)\n        NPC Tier 2: codificar via pipeline (role especifico)\n        NPC Tier 3: codificar + expandir_npc (com quest)\n        Monster: golden template ou codificar\n        Quest: expandir_npc no giver\n    \n    Args:\n        spec: dict com type, name, role, etc. (vindo de idea_to_entity_spec)\n        world_state: estado acumulado (para registrar)\n    \n    Returns:\n        dict com sucesso, entidade, arquivo, tier, erros\n    \"\"\"\n    tipo = spec.get('type', '')\n    nome = spec.get('name', '') or spec.get('title', '')\n    role = spec.get('role', '')\n\n    if not nome:\n        return {'sucesso': False, 'erro': 'Entidade sem nome', 'spec': spec}\n\n    # âââ NPC ââââââââââââââââââââââââââââââââââââââââââââââ\n    if tipo == 'npc':\n        tem_quest_associada = bool(spec.get('quest'))\n        \n        if is_template_role(role) and not tem_quest_associada:\n            return _criar_npc_template(spec)\n        else:\n            return _criar_npc_codificado(spec, tem_quest_associada)\n\n    # âââ MONSTER ââââââââââââââââââââââââââââââââââââââââââ\n    elif tipo == 'monster':\n        return _criar_monstro"
      ],
      "variaveis": [],
      "funcoes": [],
      "tabelas": [],
      "tamanho_linhas": 49
    }
  ]
}